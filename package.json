{
  "name": "prompt-engineer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A version-controlled prompt library, published as a static site on GitHub Pages.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "node scripts/serve.mjs",
    "dev": "node scripts/build.mjs && node scripts/serve.mjs",
    "new": "node scripts/new-prompt.mjs"
  }
}
