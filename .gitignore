# config.js is safe to commit — no longer contains secrets

# node / test tooling
node_modules/
coverage/

# sale package output
poker-tracker-*.zip
.pkg_stage/

# reference docs (not part of the app)
*.pdf
poker-api/

# wrangler files
.wrangler
worker/wrangler.jsonc
.dev.vars*
!.dev.vars.example
.env*
!.env.example
